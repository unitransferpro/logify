# 로기파이 (logify) — 프로젝트 가이드

> 이 파일은 Claude Code가 세션 시작 시 자동으로 읽습니다.
> 두 사람이 같은 저장소에서 협업하므로, 아래 규칙을 **반드시** 지켜 주세요.

---

## 1. 이 프로젝트가 뭔가

로기파이의 **회사 랜딩 사이트**입니다. 앱이 아니라 웹사이트입니다.

- **저장소:** `unitransferpro/logify` (GitHub, **public**)
- **라이브:** https://logify.co.kr (옛 주소 `unitransferpro.github.io/logify/` 는 301 리다이렉트)
- **브랜드:** Logify / 로기파이 / "시장의 빈틈을 찾아 앱으로 메웁니다" / 잉크 `#0d0f14` + 블루 `#3b5bdb` + 펄 배경 `#eef1f8`

### 이 사이트가 앞으로 할 두 가지 역할

1. **일반 방문자용 랜딩 (현재 만들어져 있는 부분)**
   회사 소개, 일하는 방식, 우리가 만든 앱 소개, 앱 다운로드/바로가기 링크, 제휴·투자·채용 문의.
2. **우리용 관리자 콘솔 (아직 없음 · 앞으로 추가)**
   로그인 후 각 앱을 코드 수정·재배포 없이 운영하는 화면.
   예: 사용자 관리, 신고된 댓글·게시글 삭제, 공지 띄우기, 지표 확인.
   → 설계 방향은 **6번 항목**에 정리해 뒀습니다. 만들기 전에 꼭 읽어 주세요.

즉 지금은 정적 사이트지만, **기능이 계속 붙는 전제**로 코드를 짜세요.
새 페이지를 추가할 땐 기존 4개 HTML과 같은 구조(나브 · 푸터 · `?v=` 캐시버스트)를 그대로 따릅니다.

---

## 2. 구조

```
index.html      랜딩 (히어로 · 비전 · 통계 · 앱 소개 · 가치 · 로드맵 · CTA)
contact.html    문의 페이지 (폼 → mailto 로 메일 작성)
privacy.html    개인정보처리방침
terms.html      이용약관
assets/
  styles.css    전체 스타일 (단일 파일, :root 에 디자인 토큰)
  app.js        전체 스크립트 (단일 파일, 즉시실행 함수 하나)
  favicon.svg
  shots/        앱 스크린샷 (phone mockup 안에 들어가는 이미지)
.nojekyll       GitHub Pages 의 Jekyll 처리 비활성화 (지우지 마세요)
```

**빌드 도구·프레임워크·npm 의존성이 전혀 없습니다.** 파일 수정 → 새로고침 끝.
외부 의존은 CDN 3개뿐입니다: Google Fonts(Space Grotesk, JetBrains Mono), jsDelivr(Pretendard).

`assets/app.js` 안에 들어있는 것(위에서 아래 순서대로):
`[data-year]` 연도 스탬프 · 나브 스크롤 상태 + 모바일 시트 · `.rv` 스크롤 리빌(IntersectionObserver) ·
`[data-count]` 숫자 카운트업 · 로드맵 타임라인 점등 · 히어로 캔버스(`#gapfield`, "빈틈을 찾는" 레티클 애니메이션) ·
문의 폼 검증 + mailto 조립.

기능을 추가할 때도 **이 두 파일 안에** 같은 패턴으로 넣으세요. 파일을 쪼개지 마세요(관리자 콘솔은 예외, 6번 참고).

---

## 3. 배포 (중요)

**`main`에 push하면 GitHub Pages가 자동 배포합니다.** 워크플로 파일 없이 Pages 기본 빌드(`main` 브랜치 `/` 경로)를 씁니다.

```bash
git push origin main    # → 보통 1분 내 라이브 반영
```

배포 상태 확인:

```bash
gh api repos/unitransferpro/logify/pages/builds/latest --jq '.status, .created_at'
```

### 배포 전 반드시: 캐시버스트 버전 올리기

`styles.css` 나 `app.js` 를 고쳤으면 **4개 HTML 전부**의 `?v=N` 을 +1 하세요.
안 올리면 방문자 브라우저가 옛 CSS/JS를 계속 씁니다. (pyeonipgak 의 `sw.js` 버전 올리기와 같은 역할입니다.)

```bash
# 현재 버전 확인 (4개 파일이 같은 숫자여야 정상)
grep -h '?v=' *.html | sort -u

# 다음 숫자로 올리기 (예: 3 → 4)
sed -i 's/?v=3/?v=4/g' index.html contact.html privacy.html terms.html
```

### GitHub 계정 주의 (자주 걸림)

이 저장소는 **`unitransferpro`** 계정으로 push해야 합니다. 다른 계정이 활성화돼 있으면 인증 오류로 실패합니다.

```bash
gh auth status                                   # 활성 계정 확인
gh auth switch --user unitransferpro             # 필요 시 전환
git -c credential.helper='!gh auth git-credential' push origin main
```

### 배포 검증

Pages 도 CDN 캐시가 있어 파일별로 시차가 있습니다. 항상 캐시 우회로 확인하세요.

```js
fetch(url + '?t=' + Date.now(), {cache:'reload'})
```

한쪽 파일만 구버전으로 보이면 배포 실패가 아니라 캐시 시차인 경우가 대부분입니다. 잠시 후 다시 확인하세요.

### 되돌리기 어려운 동작은 먼저 물어보세요

- **`main` push 는 곧 대외 공개입니다.** 사용자가 "배포해"라고 명시하기 전에는 push하지 마세요.
- 커밋·push, 도메인 설정 변경, Pages 설정 변경, 저장소 공개범위 변경은 전부 **먼저 확인**받고 실행합니다.

---

## 4. 도메인 (`logify.co.kr`) — 연결 완료

**2026-07-28 연결 완료.** 이제 손댈 일이 거의 없습니다. 아래는 구성 기록입니다.

- 등록기관 · DNS: **가비아** (네임서버 `ns.gabia.net` 등)
- A 레코드 4개: `@` → `185.199.108.153` / `.109.153` / `.110.153` / `.111.153` (GitHub Pages)
- `www` CNAME → `unitransferpro.github.io.` (`www` 는 apex 로 301)
- `google-site-verification` TXT 레코드 (구글 서치콘솔) — **지우지 마세요**
- 저장소 루트 `CNAME` 파일 (내용 `logify.co.kr`) — GitHub 이 자동 생성. **지우지 마세요.** 지우면 커스텀 도메인이 풀립니다.
- HTTPS: Pages `https_enforced=true`. 인증서는 GitHub 이 무료 발급 · 자동 갱신 (손댈 것 없음)

상태 확인:

```bash
gh api repos/unitransferpro/logify/pages --jq '{cname,status,https_enforced,html_url}'
```

**주의**

- 도메인 · DNS · Pages 설정 변경은 **전부 사용자 확인 후** 실행하세요.
- 주소가 `/logify/` 에서 루트 `/` 로 바뀌지만, 내부 링크가 전부 상대 경로라 안 깨집니다. **새 페이지도 절대 경로(`/assets/...`)가 아니라 상대 경로로 쓰세요.**
- `www` CNAME 은 GitHub 을 가리킵니다. 나중에 저장소를 삭제 · 비공개 전환하거나 Custom domain 을 해제하면 **DNS 레코드도 같이 지우세요.** 방치하면 서브도메인 탈취 위험이 있습니다.
- 나중에 관리자 콘솔 백엔드(`api.logify.co.kr` 같은 Worker)가 필요해지면 네임서버만 Cloudflare 로 옮기면 됩니다. 레코드가 같으면 다운타임 없이 전환되고, 등록기관은 `.co.kr` 이라 가비아에 그대로 남습니다. **그 필요가 생기기 전에 미리 옮기지 마세요.**

---

## 5. 둘이서 같이 작업하는 규칙

> 지금 이 저장소는 **두 사람이 동시에** 작업합니다.

1. **작업 시작 전 `git pull`. push 직전에 또 `git pull`.** 이거 하나만 지켜도 문제 대부분이 안 생깁니다.
2. **push가 거부되면**(non-fast-forward) → `git pull`(머지) → 충돌 있으면 해결 → 다시 push. **작업이 사라지지 않습니다.**
3. **파일/영역을 나눠서 작업하세요.** `assets/styles.css` 와 `assets/app.js` 는 거대한 단일 파일이라 동시에 만지면 충돌 확률이 높습니다.
   예: 한 명은 랜딩 섹션(index.html + 해당 CSS 블록), 다른 한 명은 문의/약관 페이지.
4. **`?v=N` 캐시버스트 줄은 거의 항상 충돌합니다.** 충돌 시 **더 큰 숫자**를 채택하고, 4개 HTML을 같은 숫자로 맞추세요.
5. CSS 를 추가할 땐 파일 끝에 몰아넣지 말고 **관련 섹션 블록 안에** 넣으세요(`/* ==== APPS ==== */` 식 주석 구획을 따릅니다). 충돌 범위가 줄어듭니다.
6. 커밋 메시지 끝에 다음 줄을 넣습니다:
   `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`

---

## 6. 관리자 콘솔 (앞으로 만들 것) — 설계 전 필독

목표: **코드 수정·재배포 없이** 앱들을 운영. 사용자 관리, 신고 댓글·게시글 삭제, 공지, 지표 확인.

### 저장소가 public 입니다 — 이게 제약의 핵심

- **비밀값을 커밋하지 마세요.** 패스코드, API 키, 토큰, 서비스 계정, DB 접속 정보 전부 금지.
  프론트엔드 JS 에 넣는 것도 커밋과 똑같이 공개입니다.
- **사용자 데이터를 이 저장소에 넣지 마세요.** 회원 목록, 이메일, 게시글 덤프, 스크린샷 속 실제 개인정보 포함.
- 인증·권한 판정은 **전부 서버에서** 합니다. `if (password === '...')` 같은 프론트 검증은 잠금장치가 아닙니다.

### 아키텍처 방향

GitHub Pages 는 정적 호스팅이라 백엔드가 없습니다. 그래서 관리자 콘솔은 **각 앱의 백엔드 API 를 호출하는 클라이언트**로 만듭니다.

- 편입로그는 이미 Cloudflare Worker + KV 백엔드와 관리자 API 가 있습니다
  (`POST /api/admin/login {pass}` → 단기 토큰 → 이후 `x-admin-token` 헤더).
  **새 인증 체계를 발명하지 말고 이 패턴을 재사용하세요.**
- 다른 앱들은 지금 GitHub Pages 정적 배포입니다. 그 앱들을 관리하려면 **그 앱 쪽에 백엔드가 먼저 필요합니다.**
  이 저장소만 고쳐서 되는 일이 아니니, 착수 전에 사용자와 범위를 맞추세요.
- 토큰은 메모리나 `sessionStorage` 에. 크로스 오리진 호출이므로 각 Worker 에 **CORS 허용 오리진**을 명시적으로 추가해야 합니다.

### 화면 규칙

- 경로는 별도 파일로 분리하세요: `admin.html` + `assets/admin.js` + `assets/admin.css`.
  랜딩용 `app.js` / `styles.css` 를 관리자 코드로 오염시키지 마세요. (동시 작업 충돌도 줄어듭니다.)
- 관리자 페이지는 검색엔진에서 빼세요: `<meta name="robots" content="noindex,nofollow">`.
- **삭제·차단·공지 발송처럼 되돌리기 어려운 동작은 확인 단계를 넣으세요.** 한 번 클릭으로 실행되게 만들지 마세요.
- 랜딩 나브에 관리자 링크를 노출하지 마세요. 주소를 직접 입력해 들어갑니다.

---

## 7. 회사·앱 정보 정직성 — 타협 불가

이 사이트는 **대외 공개되는 회사 공식 정보**입니다. 추측·날조 금지.

- **사업자 정보(상호·대표·사업자등록번호·업태·개업일·주소)는 실제 사업자등록증 값만.** 확인 없이 고치지 마세요.
- **앱 상태 배지(`Live` / `Beta` / `계획`)는 실제 상태와 맞춰야 합니다.** 아직 안 나온 걸 `Live` 로 쓰지 않습니다.
  로드맵의 미래 항목은 `계획` 배지를 유지하세요.
- **통계 숫자(`data-count`)를 임의로 늘리지 마세요.** 지금 값은 서비스 4개 / 자체 개발 100% / 설립 2026 입니다. 근거가 바뀌면 같이 바꿉니다.
- **앱 기능 목록(`feat-list`)에 없는 기능을 적지 마세요.** 실제로 그 앱에 있는 기능만.
- 앱 바로가기 링크는 실제로 열리는 주소만 씁니다. 죽은 링크·미출시 앱 링크를 넣지 마세요.
- 다운로드 버튼(스토어 링크)은 **스토어에 실제 등재된 뒤** 추가합니다.

### 앱 목록은 4개입니다

**편입로그 · meetlog · 라스트티 · 펫시세.** 이게 전부입니다.
(2026-07-28: meta description 에 남아 있던 **대시부스터**는 우리 앱이 아니어서 삭제했고, `styles.css` 의 미사용 `--h-dash` 변수도 함께 정리했습니다.)

앱을 추가 · 제거할 땐 **네 곳을 같이** 맞추세요. 한 곳만 고치면 또 어긋납니다.

1. `index.html` 의 앱 소개 섹션 (`article.app-feature`)
2. `index.html` 의 meta description
3. 통계의 `data-count="4"` (운영 중인 서비스 개수)
4. 4개 HTML 전부의 푸터 `Services` 링크 목록

앱마다 고유 색은 `styles.css` 의 `:root` 에 `--h-앱이름` 으로 정의하고, `article.app-feature` 의 `style="--hue:var(--h-앱이름)"` 로 연결합니다. **앱을 빼면 그 변수도 지우세요.**

---

## 8. 자주 밟는 지뢰

**`?v=` 안 올리고 배포** — 가장 흔합니다. CSS/JS 고쳤으면 4개 HTML 전부 올리세요. (3번 항목)

**나브·푸터가 4개 파일에 복붙돼 있음** — 공용 컴포넌트가 없습니다. 메뉴 항목이나 푸터 링크를 바꿨으면
`index.html` · `contact.html` · `privacy.html` · `terms.html` **전부** 고쳐야 합니다. 한 곳만 고치면 페이지마다 메뉴가 달라집니다.

**`.rv` 안 붙이면 안 보임 / 붙이고 안 걸리면 안 보임** — 스크롤 리빌은 `.rv` → `.in` 토글입니다.
새 섹션에 `.rv` 를 붙였는데 IntersectionObserver 가 안 걸리는 위치(예: 처음부터 화면 밖 아닌 곳, `display:none` 컨테이너 안)면 영구히 숨습니다.
`app.js` 에 1200ms 후 `body.loaded` 폴백이 있는 건 히어로 전용이니, 새 섹션은 실제 스크롤로 확인하세요.

**히어로 캔버스와 텍스트 겹침** — `pickGap()` 이 레티클을 오른쪽 영역에만 두도록 좌표를 제한하고 있습니다(`xMin = W * 0.56`, 좁은 화면은 `0.28`).
히어로 카피 레이아웃을 바꾸면 이 값도 같이 조정해야 텍스트를 안 침범합니다.

**`prefers-reduced-motion` 경로를 잊음** — `app.js` 의 애니메이션은 전부 `reduce` 분기가 있습니다.
새 애니메이션을 넣을 때도 정적 대안을 같이 넣으세요(안 넣으면 해당 사용자에겐 아무것도 안 보일 수 있습니다).

**문의 폼은 서버가 없습니다** — `mailto:minjoon@logify.co.kr` 로 메일 클라이언트를 띄우는 방식입니다.
"제출됐다"는 확인 메시지가 뜨지만 실제 전송은 사용자 메일 앱이 합니다. 폼을 손볼 때 이 한계를 잊지 마세요.
실제 서버 접수로 바꾸려면 백엔드가 필요합니다(6번 항목과 같은 문제).

**`.nojekyll` 삭제 금지** — 지우면 GitHub Pages 가 Jekyll 처리를 하면서 `_` 로 시작하는 경로 등이 깨질 수 있습니다.

**CDN 폰트 의존** — Pretendard 와 Google Fonts 가 막히는 환경에선 폰트가 폴백됩니다.
`--f-display` / `--f-body` 에 system-ui 폴백이 이미 들어 있으니, 폰트 스택을 줄이지 마세요.

---

## 9. 로컬에서 보기

빌드가 없으므로 저장소 루트를 정적 서버로 띄우면 됩니다.

```bash
python -m http.server 8788
# → http://127.0.0.1:8788
```

`.claude/launch.json` 에 `logify-static` 설정이 있어서, Claude Code 의 preview 도구로도 띄울 수 있습니다.
단 **다른 세션이 이미 다른 폴더에서 정적 서버를 돌리고 있으면 포트 충돌로 실패**합니다. 그때는 위 커맨드로 직접 띄우세요.

`file://` 로 직접 열지 마세요. 상대 경로와 캔버스·폰트가 정상 동작하지 않을 수 있습니다.

정적 서버는 캐시 헤더가 없어 브라우저가 옛 CSS/JS를 계속 쓸 수 있습니다. 확인이 안 되면 하드 리로드하세요.

---

## 10. 작업할 때 지켜 주세요

- 기존 코드 스타일을 따르세요(주석 밀도, 섹션 구획 주석, 네이밍, 한국어 UI 문구 톤).
- **JS 는 지금 스타일 그대로:** ES5 풍 `var` + `function`, IIFE 하나, 빌드 없음, 의존성 없음. 트랜스파일이 필요한 문법을 넣지 마세요.
- **CSS 는 `:root` 디자인 토큰을 쓰세요.** 색상 하드코딩 대신 `var(--accent)`, `var(--ink)`, `var(--radius)` 등.
- UI 문구는 **한국어**, 존댓말, 짧고 담백하게. em dash 남용 금지.
- 새 섹션을 추가하면 나브(데스크톱 `ul.nav-links` + 모바일 `.nav-mobile`)와 푸터 링크를 **4개 HTML 전부** 동기화하세요.
- 이미지는 `assets/shots/` 에, `width`/`height` 명시 + `loading="lazy"` + 의미 있는 `alt`. 레이아웃 점프를 막습니다.
- 접근성을 지키세요: 장식 요소엔 `aria-hidden="true"`, 토글엔 `aria-expanded`, 링크엔 알아볼 수 있는 텍스트.
- 변경 후에는 실제로 **브라우저에서 동작을 확인**하고(문법 체크만으로 끝내지 말 것), 데스크톱·모바일 폭 둘 다 보세요. 그 다음에 배포합니다.
- 되돌리기 어렵거나 대외로 나가는 동작(push, 도메인 변경, 관리자 콘솔의 삭제·공지 발송)은 **먼저 물어보고** 실행하세요.
